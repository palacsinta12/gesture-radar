[project]
name = "gesture-radar"
version = "0.1.0"
description = "FMCW radar gesture recognition pipeline with a TensorFlow CNN and DSP preprocessing."
readme = "README.md"
requires-python = ">=3.10,<3.12"
authors = [
  { name = "Your Name" }
]
license = { file = "LICENSE" }
dependencies = [
  "numpy>=1.26",
  "matplotlib>=3.8",
  "scikit-learn>=1.3",
  "tensorflow>=2.12,<2.16"
]

[tool.pytest.ini_options]
pythonpath = ["."]
